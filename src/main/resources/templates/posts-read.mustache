<!DOCTYPE HTML>
<html>
<head>
    <title>스프링 부트 웹서비스</title>
    <meta http-equiv="Content-Type" content="text/html charset=UTF-8"/>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Black+Han+Sans&family=Noto+Serif+KR:wght@200&display=swap" rel="stylesheet">
    <style>
        .default{
            width:880px;
            float: right;
        }
        .Black-Han-Sans{
            font-family:'Black Han Sans'; sans-serif;
        }
        .Noto-Serif-Korean{
            font-family:'Noto Serif KR'; serif;
        }
    </style>
</head>
<body>
{{>layout/header}}
<div class="col-md-12">
    <!-- 로그인 가능 영역-->
    <div class="form-inline row" style="background-color:#40E0D0; border:8px solid black;">
        <div class="Black-Han-Sans">
            <h1 style="color:#FFFFFF">"스프링 부트로 시작하는 웹 서비스 Ver.2"</h1>
        </div>
        <div class = "col-md-12">
            <div class="form-inline col-md-12">
                <div class="Noto-Serif-Korean" style="float:left;">
                    {{#userNames}}
                        Logged in as: <span id="name">{{userNames}}</span>
                        <a href ="/logout" class ="btn btn-info active" role="button">Logout</a>
                        <a href="/posts/save" role="button" class="btn btn-primary">글 등록</a>
                        {{#userNamesLength}}
                        <div style="float:right; width:{{userNamesLength}}px;">
                        {{/userNamesLength}}
                        <form style="float:right" action="/posts/search" method="GET" class="form-inline p-2 bd-highlight" role="search">
                            <input type="text" name="keyword" class="form-control" id="search" placeholder="검색">
                            <button class="btn btn-success bi bi-search"></button>
                        </form>
                    </div>
                    {{/userNames}}
                    {{^userNames}}
                        <a href="/oauth2/authorization/google" role="button"><img src="https://developers.google.com/identity/images/btn_google_signin_light_normal_web.png?hl=ko"></a>
                        <a href="/oauth2/authorization/naver" role="button"><img src={{Naver_img}} height="40"></a>
                        <a href="/oauth2/authorization/kakao" role="button"><img src="https://developers.kakao.com/tool/resource/static/img/button/login/full/en/kakao_login_medium_narrow.png"></a>
                        <div class="default">
                            <form style="float:right" action="/posts/search" method="GET" class="form-inline p-2 bd-highlight" role="search">
                                <input type="text" name="keyword" class="form-control" id="search" placeholder="검색">
                                <button class="btn btn-success bi bi-search"></button>
                            </form>
                        </div>
                    {{/userNames}}
                </div>

            </div>
        </div>
    </div>
    <div class="col-md-6">
        <div class="form-group">
            < type="text" class="form-control" id="title" value="{{post.title}}" readonly>
        </div>
        <div class="form-group">
            <label for="author"> 작성자 </label>
            <input type="text" class="form-control" id="author" value="{{post.author}}" readonly>
        </div>
        <div class="form-group">
            <label for="content"> 내용 </label>
            <textarea class="form-control" id="content" readonly>{{post.content}}</textarea>
        </div>
        </form>
        <a href="/" role="button" class="btn btn-secondary">취소</a>
        <button type="button" class="btn btn-primary" id="btn-update">수정 완료</button>
        <button type="button" class="btn btn-danger" id="btn-delete">삭제</button>
    </div>
</div>

{{>layout/footer}}